---
apiVersion: v1
kind: Service
metadata:
  name: k8s-project5-LoadBalancer
  labels:
    app: k8s-project5-LoadBalancer
spec:
  selector:
    app: myapp-project5
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000